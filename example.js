var undoing = require('./')
//var css = ".header{font-size: 24px;margin-bottom:10px;}.header{margin-bottom:0;}"
var css = ".header{font-size: 24px;margin-bottom:10px;}.heder{margin-bottom:0;}"

undoing.check(css);
